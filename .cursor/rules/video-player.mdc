---
name: video-player
description: 视频播放器核心功能开发规范。用于开发播放、暂停、seek、手势控制、画中画、音轨切换、硬件解码等功能时触发。
globs:
  - "**/Player/**/*.swift"
  - "**/Core/Player/**/*.swift"
---

# 视频播放器模块

## 功能清单（参考 Infuse）

### 播放核心
- [ ] 支持多格式播放：MKV, MP4, AVI, WMV, MOV, FLV, RMVB, TS, M2TS
- [ ] 硬件解码优先，软解回退 (AVFoundation + FFmpeg)
- [ ] 支持 HDR (Dolby Vision, HDR10, HLG)
- [ ] 支持 Dolby Atmos / DTS / 多声道音频
- [ ] 播放进度记忆（断点续播）
- [ ] 多音轨切换
- [ ] 播放速度调节 (0.5x ~ 4.0x)
- [ ] 章节导航 (Chapter Navigation)
- [ ] A-B 循环播放
- [ ] 连续播放 / 自动播放下一集

### 播放器手势（参考 Infuse）
- [ ] 左右滑动：快进/快退（显示时间预览）
- [ ] 左侧上下滑动：调节亮度
- [ ] 右侧上下滑动：调节音量
- [ ] 双击：播放/暂停
- [ ] 捏合手势：画面缩放 (Fit / Fill / Stretch)
- [ ] 长按：倍速播放

### 画中画 & AirPlay
- [ ] 系统 PiP (Picture-in-Picture)
- [ ] AirPlay 投屏
- [ ] 后台音频播放

## 播放引擎架构

```swift
protocol PlayerEngine: AnyObject {
    var state: PlayerState { get }
    var currentTime: CMTime { get }
    var duration: CMTime { get }
    var playbackRate: Float { get set }

    func load(url: URL, startPosition: CMTime?) async throws
    func play()
    func pause()
    func seek(to time: CMTime) async
    func selectAudioTrack(_ track: AudioTrack)
    func selectSubtitleTrack(_ track: SubtitleTrack)
}
```

## 开发规范

1. **播放器状态机**：使用枚举定义明确的状态 (idle, loading, playing, paused, buffering, error, ended)
2. **KVO 观察**：AVPlayer 属性观察使用 Combine Publisher 包装，避免裸 KVO
3. **后台任务**：进入后台时保存播放进度，使用 BGTaskScheduler
4. **内存管理**：播放器销毁时清理所有观察者和定时器
5. **错误恢复**：网络中断自动重连，解码失败切换到软解
