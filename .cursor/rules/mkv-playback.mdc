---
alwaysApply: false
---
# MKV 视频解析与播放

## 背景

当前 `AVPlayerEngine` 基于 AVFoundation，不支持 MKV 容器格式。需要引入 FFmpeg 实现 MKV 的解封装(demux)与解码(decode)，同时保留 AVFoundation 处理原生格式以获得最佳功耗和系统集成（PiP、AirPlay）。

## 技术方案：双引擎架构

```
PlayerEngine (协议)
├── AVPlayerEngine      ← MP4/MOV/M4V 等原生格式（已有）
└── FFmpegPlayerEngine   ← MKV/AVI/RMVB/FLV 等非原生格式（新增）
```

根据文件扩展名自动选择引擎，对上层 `PlayerViewModel` 完全透明。

## 第一步：引入 FFmpegKit

通过 SPM 引入 `ffmpeg-kit-ios-full` (GPL 版本，包含所有编解码器)：

```swift
// Package.swift 或 Xcode 中 Add Package
// URL: https://github.com/arthenica/ffmpeg-kit
// 选择 ffmpeg-kit-ios-full 产品
```

如果 FFmpegKit 不适用（GPL 许可或包体积问题），可考虑：
- **MobileVLCKit**（LGPL，通过 CocoaPods: `pod 'MobileVLCKit'`）
- **libmpv**（通过 SPM 或手动编译）

无论选择哪个，都必须实现 `PlayerEngine` 协议。

## 第二步：新增文件清单

| 文件路径 | 职责 |
|----------|------|
| `Core/Player/FFmpegPlayerEngine.swift` | FFmpeg 播放引擎，实现 `PlayerEngine` 协议 |
| `Core/Player/MKVDemuxer.swift` | MKV 容器解封装，提取音频/视频/字幕轨道 |
| `Core/Player/VideoRenderer.swift` | 将解码帧渲染到 `CALayer`/`MTKView` |
| `Core/Player/AudioRenderer.swift` | PCM 音频输出，使用 `AVAudioEngine` 或 `AudioUnit` |
| `Core/Player/HardwareDecoder.swift` | VideoToolbox 硬件解码封装 |
| `Core/Player/PlayerEngineFactory.swift` | 根据 URL/格式自动选择引擎 |

## 第三步：PlayerEngineFactory 选择策略

```swift
enum PlayerEngineFactory {
    static func engine(for url: URL) -> PlayerEngine {
        let ext = url.pathExtension.lowercased()
        let nativeFormats: Set<String> = ["mp4", "mov", "m4v", "mp3", "m4a"]
        if nativeFormats.contains(ext) {
            return AVPlayerEngine()
        } else {
            return FFmpegPlayerEngine()
        }
    }
}
```

在 `PlayerViewModel.init` 中使用工厂替代直接创建：

```swift
init(item: MediaItem) {
    self.item = item
    self.engine = PlayerEngineFactory.engine(for: item.fileURL)
    setupBindings()
}
```

## 第四步：FFmpegPlayerEngine 实现要点

### 4.1 解封装 (Demux)

```swift
// 使用 avformat_open_input 打开 MKV
// avformat_find_stream_info 获取流信息
// 遍历 AVFormatContext.streams 识别视频/音频/字幕轨道
// av_read_frame 循环读取 AVPacket
```

关键点：
- MKV 可能包含多条音频轨（不同语言）和多条字幕轨，必须全部枚举
- 章节信息从 `AVFormatContext.chapters` 读取
- 附件（字体文件等）从 `AV_DISPOSITION_ATTACHED_PIC` 识别

### 4.2 解码策略

优先硬解，失败回退软解：

```swift
// 1. 尝试 VideoToolbox 硬件解码
//    - 创建 VTDecompressionSession
//    - 支持 H.264/H.265/VP9 硬件加速
// 2. 回退 FFmpeg 软解
//    - avcodec_find_decoder → avcodec_open2
//    - avcodec_send_packet → avcodec_receive_frame
```

### 4.3 渲染管线

```
AVPacket → Decoder → AVFrame → CVPixelBuffer → MTKView/CALayer
```

- 视频帧转换为 `CVPixelBuffer`，通过 Metal 渲染到 `MTKView`
- 音频帧转换为 PCM，通过 `AVAudioEngine` 播放
- 音视频同步：以音频时钟为基准，视频帧按 PTS 对齐

### 4.4 必须实现的 PlayerEngine 协议方法

```swift
final class FFmpegPlayerEngine: PlayerEngine {
    // statePublisher / currentTimePublisher / durationPublisher / bufferProgressPublisher
    // load(url:startPosition:) — 打开文件，解析流信息
    // play() / pause() / stop() — 控制解码线程
    // seek(to:) — avformat_seek_file，flush 解码器缓冲区
    // selectAudioTrack(index:) — 切换音频流索引
    // selectSubtitleTrack(index:) — 切换字幕流索引或禁用
    // availableAudioTracks() — 从 AVFormatContext 枚举音频流
    // availableSubtitleTracks() — 从 AVFormatContext 枚举字幕流
}
```

### 4.5 线程模型

```
主线程 (Main)        — UI 更新、状态发布
解封装线程 (Demux)    — av_read_frame 循环
视频解码线程 (Video)  — 解码 + 渲染
音频解码线程 (Audio)  — 解码 + 播放
```

使用 Swift Actor 或 GCD 串行队列隔离各线程状态，避免数据竞争。

## 第五步：MKV 特有功能

### 5.1 多轨道信息提取

从 `AVStream` 元数据中提取：
- `language` 标签 → 映射到 `AudioTrackInfo.language` / `SubtitleTrackInfo.language`
- `title` 标签 → 映射到 `displayName`
- `codec_id` → 映射到 `AudioTrackInfo.codec`
- `channels` → 映射到 `AudioTrackInfo.channels`

### 5.2 内嵌字幕提取

MKV 常见内嵌字幕格式：ASS/SSA、SRT、PGS (sup)、VobSub

```swift
// 文本字幕 (ASS/SRT)：解码 AVPacket → 转为 String → 复用现有 SubtitleManager
// 图形字幕 (PGS/VobSub)：解码为位图 → 叠加渲染到视频层上方
```

对文本字幕，将解码后文本转换为现有 `SubtitleManager` 能处理的格式（SRT/VTT），复用已有字幕渲染逻辑。

### 5.3 章节导航

```swift
struct Chapter: Identifiable {
    let id: Int
    let title: String
    let startTime: CMTime
    let endTime: CMTime
}
```

从 `AVFormatContext.chapters` 提取，在 `PlayerViewModel` 中暴露 `chapters: [Chapter]` 供 UI 使用。

## 第六步：与现有组件的集成

### 6.1 PlayerView 适配

`PlayerView` 中的 `VideoPlayerLayer`（当前包装 `AVPlayerLayer`）需要条件切换：
- AVPlayerEngine → 使用 `AVPlayerLayer`
- FFmpegPlayerEngine → 使用 `MTKView` 或自定义 `CALayer`

建议抽象为 `VideoRenderView` 协议，两种引擎各自提供渲染视图。

### 6.2 远程 MKV 流式播放

WebDAV/SMB 返回的 `streamURL` 可直接传给 FFmpegPlayerEngine：
- FFmpeg 的 `avformat_open_input` 原生支持 HTTP(S) URL
- 对于 SMB，需要先通过自定义 `AVIOContext` 接入 SMB 读取流

### 6.3 PlayerViewModel 无需改动

由于 `FFmpegPlayerEngine` 实现相同的 `PlayerEngine` 协议，`PlayerViewModel` 的所有逻辑（手势、进度保存、轨道选择等）无需修改。

## 开发规范

1. **协议一致性**：`FFmpegPlayerEngine` 必须完整实现 `PlayerEngine` 协议所有方法，行为语义与 `AVPlayerEngine` 保持一致
2. **内存管理**：FFmpeg 分配的 `AVFormatContext`、`AVCodecContext`、`AVFrame`、`AVPacket` 必须在 `stop()` 和 `deinit` 中释放
3. **错误处理**：FFmpeg 返回的错误码（负数）转换为 `PlayerError`，提供用户友好的中文错误信息
4. **性能**：视频解码使用硬件加速（VideoToolbox），只在不支持的编码格式上回退软解
5. **日志**：解码过程的关键节点使用 `VanmoLogger.player` 记录，便于排查问题
6. **C 桥接**：FFmpeg C API 通过 Swift 包装类调用，不在业务层直接使用裸 C 函数
7. **测试**：为 `MKVDemuxer` 和 `PlayerEngineFactory` 编写单元测试，使用本地 MKV 样本文件
