---
name: ui-design
description: Vanmo UI/UX 设计规范。用于开发界面、组件、动画、主题等时触发。
globs:
  - "**/Views/**/*.swift"
  - "**/Components/**/*.swift"
---

# UI/UX 设计规范

## 设计语言（参考 Infuse 风格）

Vanmo 追求 **电影感** 的沉浸式视觉体验：

### 视觉风格
- **深色主题为主**：以深色/黑色背景突出影片海报和内容
- **大图优先**：封面、背景图占据视觉主体，文字信息精简
- **毛玻璃效果**：使用 `.ultraThinMaterial` 等材质营造层次感
- **流畅动画**：页面转场、列表滚动要有丝滑的动画和弹性效果
- **无边框设计**：最大化内容展示区域

### 布局参考（Infuse 风格）
1. **首页**：顶部大横幅（最近/推荐）+ 横向滚动分类列表
2. **媒体库**：网格布局展示海报封面，支持列表/网格切换
3. **详情页**：背景模糊大图 + 信息覆盖层（标题、评分、简介、演员）
4. **播放器**：全屏沉浸，控件自动隐藏，手势操作
5. **设置页**：分组列表，简洁清晰

### 色彩系统

```swift
extension Color {
    static let vanmoPrimary = Color("AccentColor")
    static let vanmoBackground = Color(.systemBackground)
    static let vanmoSurface = Color(.secondarySystemBackground)
    static let vanmoOnSurface = Color(.label)
    static let vanmoSubtext = Color(.secondaryLabel)
}
```

### 排版

- 标题：`.title` / `.title2`，使用 `.bold()`
- 正文：`.body`
- 辅助信息：`.caption`，使用 `.secondary` 颜色
- 自定义字体可选：SF Pro (系统) 为默认

## 组件规范

### 海报卡片 (PosterCard)
- 圆角 12pt
- 阴影效果
- 长按显示快捷菜单（播放、收藏、详情）
- 底部渐变遮罩 + 标题文字

### 播放器控件 (PlayerControls)
- 3 秒无操作后自动隐藏
- 底部：进度条 + 时间 + 全屏
- 中部：上一个 / 播放暂停 / 下一个
- 顶部：返回 + 标题 + 设置（音轨/字幕）

## 开发规范

1. **响应式布局**：支持 iPhone 和 iPad，使用 `GeometryReader` 和 `@Environment(\.horizontalSizeClass)`
2. **无障碍**：为所有交互元素添加 `accessibilityLabel`
3. **动画**：使用 `withAnimation(.spring())` 提供弹性效果，避免硬切换
4. **图片加载**：使用 `AsyncImage` 或专用图片缓存库，提供 placeholder
5. **暗黑模式**：始终优先适配暗黑模式，亮色模式作为备选
