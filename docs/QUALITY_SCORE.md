# Quality Score

A conservative snapshot of Vanmo product domains and architectural layers. Grades reflect recorded evidence only; code existence is not treated as verified behavior.

**Last updated:** 2026-09-03  
**Current baseline evidence:** The current recorded `./init.sh` baseline completed all four stages with 0 failures: 36 `VanmoCore` tests, the CloudKit/multiplatform static check (including `./scripts/check-architecture-guards.sh` for XcodeGen drift, target source whitelist, and VanmoCore UI-import guards), the Advanced Harness documentation and live narrative-consistency check, and the iOS UI interaction CLI static check. The documentation stage now also checks init stage count, plan-index Status, spec/plan Status, and QUALITY current-baseline command paths. The UI CLI check remains structural. Focused `./scripts/check-app-build.sh ios-simulator` then passed, and `./init.sh --full` recorded a green iOS Simulator plus macOS Debug compile matrix on clean `34bf345`. Simulator XCUITest on iPhone 17 Pro recorded tree, default-screen assert, and `journey --name tab-navigation` with retained `xcresult` artifacts. No physical-device XCUITest ran for this snapshot. A separate 2026-08-26 macOS HTTP-via-Emby download acceptance run passed. Separate 2026-08-27 iOS simulator HTTP-via-Emby runs passed enqueue, live progress, completion, local play, and pause-terminate-`.part` resume. Separate 2026-08-27 Mac File Sharing runs passed SMB connect, listing, and play on iOS Simulator (KSPlayer direct `smb://`) and VanmoMac (prefetch `source=smb`). Separate 2026-08-28 encryption-required runs on VanmoMac and an iOS device recorded SMB 3.1.1 AES-128-GCM login (`dialect=0x311`, `encrypt=true`) and a `MacShare` listing. A later VanmoMac paste recorded encrypted SMB play through `[Prefetch] … source=smb` and `[MacPlayerVM] KS using prefetch proxy`. A later iOS-device paste recorded KSPlayer load of an `smb://` episode (`readyToPlay`, duration present). Separate 2026-08-28 HTTP-via-Emby play runs passed on VanmoMac (prefetch `source=http`, KSPlayer load, seeks, pause, next-episode load) and on an iOS device (`playing` / `buffering`). Those play runs are not part of `./init.sh`. A separate 2026-08-28 physical-device SMB download run passed Files-browser enqueue, same-session resume, terminate while downloading, cold-launch `.part` restore, and completion. Three 2026-08-28 Vanmo-macOS SMB runs are recorded; the third library-detail run passed the eleven-step mixed movie-and-episode matrix, including inferred single-task pause/resume, restart `.part` continuation, and Console-backed `open detail` with no second main window. An earlier Files-browser `failed` restore was local-disk exhaustion with a correct downloads-window failure state, not an engine defect. Those download and SMB runs are not part of `./init.sh`. Separate 2026-08-29 / 2026-08-30 LAN AList WebDAV `/dav` runs passed connect, listing, KSPlayer plus prefetch play, Files-browser download, and local play of the completed file on the iOS Simulator and VanmoMac. Those AList runs are not part of `./init.sh`. A separate 2026-08-30 FTP placeholder run recorded logical connect and an empty listing. A later 2026-08-31 LAN FTP run at `192.168.1.77:21` passed connect (`230`), listing (468 then 4 entries), KSPlayer plus prefetch play, Files-browser download, and local play on the iOS Simulator and VanmoMac. Those FTP runs are not part of `./init.sh`. A later 2026-08-31 LAN SFTP run at `192.168.1.77:22` passed connect (`login ok`), listing (21 then 4 entries), KSPlayer plus prefetch play (`source=sftp`), Files-browser download, and local play on the iOS Simulator and VanmoMac. Those SFTP runs are not part of `./init.sh`.

## Grading Scale

- **A:** Automation, relevant platform builds, and critical manual flows pass; boundaries are clear and no major known gap remains.
- **B:** Relevant verification passes and structure is mostly clear, but some integration or manual coverage is missing.
- **C:** Core paths have partial evidence, with explicit unverified paths or implementation gaps.
- **D:** A critical path fails, or structural problems prevent safe evolution.

## Product Domains

| Product domain | Grade | Existing evidence | Key gaps |
| --- | --- | --- | --- |
| Media identification and scanning | B | Filename, directory semantics, NFO, incremental scan, and pruning coverage is included in the passing 36-test baseline | No real large-directory, failure-recovery, or app progress-UI integration evidence |
| Downloads | B | Download model, persistence, directory resolution, and queue logic are covered by the passing baseline; one 2026-08-26 macOS HTTP-via-Emby run passed queue, pause, restart-from-part, and main-window detail navigation; 2026-08-27 iOS simulator HTTP-via-Emby runs passed enqueue, live progress, completion, local play, and pause-terminate-`.part` resume; one 2026-08-28 physical-device SMB run passed Files-browser enqueue, same-session resume, terminate while downloading, cold-launch `.part` restore, and completion; one 2026-08-28 Vanmo-macOS SMB library-detail run passed the eleven-step mixed movie-and-episode matrix; 2026-08-30 iOS Simulator and VanmoMac AList Files-browser runs each completed one download and played the local file; 2026-08-31 iOS Simulator and VanmoMac FTP Files-browser runs each completed one download and played the local file; 2026-08-31 iOS Simulator and VanmoMac SFTP Files-browser runs each completed one download and played the local file | Download journeys have no XCUITest coverage |
| Remote connections and search | C | Capability declarations and catalog playback URL behavior are covered by shared tests; 2026-08-27 Mac File Sharing runs passed connect and listing on iOS Simulator and VanmoMac; 2026-08-28 encryption-required runs on VanmoMac and an iOS device recorded SMB 3.1.1 AES-128-GCM login (`dialect=0x311`, `encrypt=true`) and a `MacShare` listing; 2026-08-28 HTTP-via-Emby catalog fetch and play ran on VanmoMac and an iOS device; 2026-08-29 / 2026-08-30 LAN AList WebDAV `/dav` connect and listing passed on the iOS Simulator and VanmoMac; 2026-08-31 LAN FTP at `192.168.1.77:21` passed connect (`230`) and listing (468 then 4 entries) on the iOS Simulator and VanmoMac; 2026-08-31 LAN SFTP at `192.168.1.77:22` passed connect (`login ok`) and listing (21 then 4 entries) on the iOS Simulator and VanmoMac | Several UI-visible protocols are placeholders or incomplete; no automated real-service suite |
| Playback and prefetching | B | Catalog URL resolution is covered and cleanup paths are documented; 2026-08-27 SMB play passed on iOS Simulator via KSPlayer and on VanmoMac via the SMB-backed prefetch proxy; 2026-08-28 encrypted SMB play recorded VanmoMac prefetch `source=smb` and an iOS-device KSPlayer `smb://` load; 2026-08-28 HTTP-via-Emby play recorded VanmoMac prefetch `source=http` with seeks/pause/next-episode and an iOS-device `playing`/`buffering` path; 2026-08-30 AList remote `.mp4` play passed on the iOS Simulator (KSPlayer, prefetch `source=http`, duration 147s) and VanmoMac (`MacKSPlayerEngine`, prefetch proxy); 2026-08-31 FTP play passed on the iOS Simulator (KSPlayer, prefetch `source=ftp`, duration 131s) and VanmoMac (`MacKSPlayerEngine`, prefetch proxy plus FTP `SIZE`/`RETR`); 2026-08-31 SFTP play passed on the iOS Simulator (KSPlayer, prefetch `source=sftp`, duration 131s) and VanmoMac (`MacKSPlayerEngine`, prefetch `source=sftp`) | No automated app playback, gesture, or player-window journey; iOS and macOS SMB play paths remain intentionally different; a macOS Emby seek logged `seek(to:)` continuation misuse |
| Metadata and subtitles | C | NFO parsing and subtitle-related shared logic have test coverage | Network refresh, cache invalidation, online subtitle, and rendering paths lack end-to-end evidence |
| Persistence and iCloud sync | C | Local/cloud store boundaries pass the static check; CloudStore schema stays CloudKit-compatible in tests; launch attaches `.private("iCloud.com.vanmo.app")` when the user toggle is on and falls back to `.none` if create throws; a 2026-09-02 signed iOS Debug device run attached `CloudStore-ck3`, created `com.apple.coredata.cloudkit.zone`, exported with `error=none` after the Apple ID iCloud upgrade, and persisted one Emby `SavedConnection`; Dashboard Development Private Database returned that `CD_SavedConnection`; a 2026-09-03 macOS Debug run showed the iOS Emby row, opened the missing-password editor (`activate type=emby missingCredential=true`), then connected after the operator entered the password (`connection-updated`) and refreshed Home | No versioned SwiftData migration; iCloud Keychain password sync is rejected; conflict merge and Mac-to-iOS import are not evidenced; Simulator Debug is not CloudKit evidence |
| iOS app experience | B | The unified XCUITest CLI, iOS Simulator Debug compile, and simulator tab-navigation journey passed on 2026-08-26 with retained `xcresult` attachments; 2026-08-27 simulator HTTP-via-Emby download success and `.part` resume flows are recorded; 2026-08-27 simulator SMB File Sharing connect, list, and KSPlayer play are recorded; one 2026-08-28 physical-device SMB download run (terminate while downloading, `.part` restore) is recorded; one 2026-08-28 physical-device encryption-required SMB KSPlayer load and one HTTP-via-Emby `playing` run are recorded; one 2026-08-30 simulator AList WebDAV connect, list, KSPlayer prefetch play, download, and local play run is recorded; one 2026-08-31 simulator FTP connect, list, KSPlayer prefetch play, download, and local play run is recorded; one 2026-08-31 simulator SFTP connect, list, KSPlayer prefetch play, download, and local play run is recorded | No signed physical-device XCUITest |
| macOS app experience | C | Platform boundaries are documented; a Debug compile of `Vanmo-macOS` passed on 2026-08-26; one HTTP-via-Emby download recovery and detail-navigation run was recorded the same day; 2026-08-27 File Sharing SMB connect, list, and prefetch play are recorded; one 2026-08-28 SMB library-detail mixed movie-and-episode download matrix is recorded; one 2026-08-28 encryption-required SMB prefetch play and one HTTP-via-Emby prefetch play (seeks, pause, next episode) are recorded; one 2026-08-30 AList WebDAV connect, list, KSPlayer prefetch play, download, and local play run is recorded; one 2026-08-31 FTP connect, list, KSPlayer prefetch play, download, and local play run is recorded; one 2026-08-31 SFTP connect, list, KSPlayer prefetch play, download, and local play run is recorded | Broader window and remaining placeholder-source journeys remain manual-only |

## Architectural Layers

| Architectural layer | Grade | Boundary and legibility evidence | Key gaps |
| --- | --- | --- | --- |
| `VanmoCore` | B | Package boundaries are explicit; 36 tests pass | Uneven service integration coverage; Swift 6 `Sendable` warnings remain |
| iOS platform layer | C | UI, navigation, and player adapters are isolated to the iOS target; Simulator XCUITest now proves tab navigation for four stable identifiers | Large orchestration surfaces remain; physical-device execution is unverified; identifier coverage is still limited to the golden journey |
| macOS platform layer | C | AppKit windows and desktop player behavior remain platform-isolated | Duplicated orchestration and manual-only window acceptance |
| SwiftData / CloudKit | C | Local/cloud model assignments and credential exclusions are documented and statically checked; CloudStore schema still creates under `.private("iCloud.com.vanmo.app")` in in-memory tests; launch uses `.private` when sync is enabled, with throw fallback to `.none`; signed-device attach, zone creation, successful export, Dashboard `CD_SavedConnection`, and macOS missing-password connect-after-import are recorded | No versioned schema or migration plan; unbound-container `CKContainer` init is still not catchable; NSPersistentCloudKitContainer reports iCloud quota as `CKError` 2 with empty userInfo |
| Build and agent workflow | B | `project.yml` is the configuration source; `./init.sh` provides four passing shared, boundary, documentation, and iOS UI CLI static stages; the documentation stage now rejects init stage-count and plan-index Status conflicts; the CloudKit/multiplatform stage now also runs `./scripts/check-architecture-guards.sh` and rejects XcodeGen drift, illegal target sources, and unconditional VanmoCore UI imports; `./init.sh --full` now records a green iOS Simulator plus macOS Debug compile matrix; simulator XCUITest is a separate evidence command | Default baseline still does not compile apps or run XCUITest; physical-device UI evidence is still missing; SwiftSyntax/taste lint is still deferred |

## Benchmark Snapshots

These snapshots measure only the stated harness path. They are not product-performance benchmarks.

| Date | Harness variant | Completion | Retries | Defects before review | Notes |
| --- | --- | --- | --- | --- | --- |
| 2026-08-25 | Advanced Harness baseline | 3/3 automated stages passed: 36 tests, static check with 0 failures, Harness documentation check with 0 failures | Not recorded | Not measured | `./init.sh`; no app build, launch, real-source flow, or CloudKit environment validation |
| 2026-08-26 | Single-root Harness | 3/3 automated stages passed: 36 tests, static check with 0 failures, Harness documentation check with 0 failures | 0 project-level retries | Not measured | Removed the parallel session-artifact layer; no app build, launch, real-source flow, or CloudKit environment validation |
| 2026-08-26 | iOS UI CLI integration | 4/4 `./init.sh` stages passed; XcodeGen generation and target discovery succeeded; simulator `simctl` screenshot succeeded | Not recorded | Existing app compile blocker recorded | `build-for-testing` stopped at the pre-existing missing `.paused` case in `SettingsView.swift:489`; no physical-device XCUITest, signing, runner-argument, attachment-export, or golden-journey evidence |
| 2026-08-26 | Debug compile evidence layer | Fast `./init.sh` 4/4 passed with no app-build evidence directory; focused macOS Debug compile passed; `./init.sh --full` recorded iOS fail + macOS pass and aggregate exit 1 | 0 | Existing iOS `.paused` compile blocker recorded | Xcode 26.0.1 / Swift 6.2; evidence under `build/app-build-evidence/runs/20260826-141002-10994`; no launch, XCUITest, or CloudKit claim |
| 2026-08-26 | iOS UI golden journey | Fast `./init.sh` 4/4 passed; focused iOS Simulator Debug compile passed; simulator `tree`, `assert`, and `journey --name tab-navigation` passed | 1 attachment-name suffix mismatch, then fixed | No new product defect | Xcode 26.0.1 / Swift 6.2; iPhone 17 Pro simulator; journey artifacts at `build/ui-cli/runs/20260826-145830-63647`; no physical-device or real-source claim |
| 2026-08-26 | Harness narrative consistency | Fast `./init.sh` 4/4 passed; `./scripts/check-harness-docs.sh` passed after link plus narrative checks | 2 checker bugs fixed before the first green run (init stage parse and word-number eval) | No new product defect | Injected RELIABILITY `five baseline stages` and active-index `Completed` for the macOS download plan; both failed as required and were restored; no app compile or XCUITest claim |
| 2026-08-26 | Debug compile evidence closeout | Fast `./init.sh` 4/4 passed with no new evidence run; focused iOS Simulator Debug compile passed; `./init.sh --full` recorded iOS pass + macOS pass and aggregate exit 0 | 0 | No new product defect | Xcode 26.0.1 / Swift 6.2; clean `34bf345`; evidence under `build/app-build-evidence/runs/20260826-153856-830`; no launch, XCUITest, or CloudKit claim |
| 2026-08-28 | Architecture structure guards | Fast `./init.sh` 4/4 passed; stage 2 ran `./scripts/check-architecture-guards.sh` | 0 | No new product defect | XcodeGen 2.44.1; injected pbxproj comment, extra macOS `Vanmo/` source, and unconditional `import SwiftUI` each failed then restored; no app compile or XCUITest claim |

Future entries should keep the command, environment, stage count, retries, and escaped defects comparable. Do not convert manual observations into a completion percentage.

## Simplification Log

Use this log for deliberate removal experiments. A result is valid only when the same verification path is run before and after the removal.

| Date | Component removed | Outcome | Decision |
| --- | --- | --- | --- |
| 2026-08-25 | None; initial log established | No simplification experiment was performed in this documentation-only unit | Preserve runtime behavior and record the first measured removal when one is attempted |
| 2026-08-26 | Parallel session-artifact documentation layer | The same `./init.sh` baseline passed after execution state and evidence ownership were consolidated under `docs/` | Keep the layer removed |

## Update Triggers

Update this score when a relevant app build, manual golden journey, real-source acceptance flow, real CloudKit check, migration strategy, automation target, or meaningful boundary change produces new evidence.
